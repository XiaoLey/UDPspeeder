## Python 项目总则

**文档写「契约」，代码与块注释写「实现」**

- 文档（docstring）只描述不随实现变化的东西：做什么、保证什么、参数与返回的语义
- 怎么做（执行步骤、内部机制）交给代码本身和代码块注释表达
- 改实现时文档不应被迫跟着改

**度的把握：砍掉真没必要的，保留真必需的，不是为砍而砍**

- 对实现的复述、执行步骤、内部机制命名、「为什么」的缘由——砍
- 改已有代码时只动必要的字，别把变更理由写进注释、把原本简短的注释扩写成一段——变更理由放 commit message
- 专业算法、领域性强、不写就看不懂的部分——必须写，该详细就详细
- 输入/输出的数据形状、字段语义等帮助理解契约的信息——保留
- 拿不准时问一句：「不写这段，读代码的人还看得懂吗？」看不懂就留，看得懂就砍

## 函数 / 类 / 结构体的文档注释

只写契约层，不写机制层。

**写：**

- 一句话目的
- 参数 / 返回值的语义（名字和类型表达不了的部分，如游标含义、枚举取值）
- 对外保证与不变式（如「单个源失败不影响其他源」「幂等」）
- 会抛出的异常及触发条件（契约粒度即可）
- 格式：需要展开参数/返回/异常时用 Google 风格（`Args:` / `Returns:` / `Raises:`）；很简单的函数一句话目的即可

**不写：**

- 执行步骤（「先构造 X，再循环 Y，最后 Z」）
- 内部机制命名（线程池 / asyncio.gather / 退避重试几轮 / 哪个 HTTP 库）
- 对代码逻辑的复述
- 「为什么需要它」的缘由——放 commit message 或设计说明
- 把通用单元绑定到特定调用方或狭窄场景（如在可复用函数的 docstring 里写「为某某客户端服务」）；归属/作用域事实（如「属于某客户端」）放模块级注释

```python
# 反例：复述执行步骤 + 点名内部机制
async def search_zhihu(*, query, count, access_secret) -> ZhihuSearchOutput:
    """调用知乎搜索接口。先构造 Bearer 头和秒级时间戳，再进入重试循环：
    发请求、检查 Code，Code==0 就 break，否则对 30001 退避重试，
    最后取 Data 字段用 from_api_data 构造输出"""

# 正例：只留目的 + 稳定的行为契约
async def search_zhihu(*, query, count, access_secret) -> ZhihuSearchOutput:
    """调用知乎搜索接口，返回结构化结果

    频率受限时自动重试，其余错误抛 RuntimeError
    """
```

## 代码块 / 逻辑块注释

- 每个逻辑块加一行简短注释，让人一眼知道这段在做什么
- 一目了然的不加（简单赋值、`i += 1`、直白的列表推导）
- 别写太详细；只有警告、坑、非显而易见的前提才多写几句
- 想写详细时，优先把大块拆成几个小块分别加注释，而不是堆一段长注释

```python
# 反例：一段长注释复述整块逻辑
# 先校验参数，再读缓存，命中就返回，没命中就请求 API，成功后写缓存，最后按 top 截断
...（一大段代码）

# 正例：拆成逻辑块，各加一行
# 校验参数
...

# 读缓存，命中直接返回
...

# 未命中：请求 API 并写缓存
...

# 按 top 截断
...
```

## 代码组织

**度的把握：不为抽而抽，多一层间接也是成本**

- 只用一次、短小直白、没有合适语义名的逻辑——内联在调用处
- 该抽的信号：多处复用、逻辑复杂或有独立含义值得命名、需要独立测试、藏起细节能明显提升可读性
- 抽出去要传一大串参数、得来回对照两处看的——多半不该抽

**抽出来的函数放最窄的适用作用域**

- 只被一个函数用就作嵌套函数；只被一个类用就放类里（如静态方法）；真被多处复用才提升到模块级
- 私有用单个前导 `_`；别用 `__` 表示「更私有」（类里触发 name mangling，模块级毫无意义）

**类成员排序**（无特殊情况按此序，可读性优先；顺序确实出人意料时加一行注释）：类级常量/数据 → 静态/类变量 → `__init__` 等双下划线/生命周期方法 → 静态公有方法（`@staticmethod`/`@classmethod`）→ 公有方法 → 静态私有方法 → 私有方法

**别手改生成的文件**——改源文件再重新生成（如由 `.ui` / `.qrc` 生成的 PySide 文件）

## 代码风格

- 保留既有风格：标识符用英文，注释文档用中文（见「书写约定」）；改已有代码时跟随周边既有写法，别顺手重排
- 项目配有 lint/format（如 ruff）时，非琐碎改动后跑一遍

## 书写约定

- 语言：注释、说明、文档尽量用中文；专业术语保留英文（如 MCP、FastMCP、pydantic、stdio、httpx）
- 句号：单行内容（单行注释、单行说明）末尾不带句号；多行内容或完整段落才在句末带句号

## 文档同步

- 改了用户可见的行为(新增/修改命令行选项、输出格式、配置项、默认值等)，同步更新对应用户文档(如 README)
- 有多份语言版本时(如中英文 README)，一起更新，别只改一份
- 文档写清用法和注意事项即可，不写实现细节、不写长篇大论

## Git Commit

**精简写要点，不复述代码**

- message 写「改了什么」，必要时写「为什么」；不说空话；不复述代码执行逻辑、不贴实现细节、不伞状概括——那些看代码和注释就行了
- 一个提交只做一件事；不相干的改动拆成多个提交，别攒成一条再靠分点罗列
- 默认一句话搞定；仅当改动较多、一句话说不清时才「一句话总述 + 分点」（每点一行、简单明确）。一两处小改动别分点，更别为凑分点把一句话拆成几条
- 常用前缀：`feat:` 新功能 / `fix:` 修 bug / `refactor:` 重构 / `chore:` 杂务 / `docs:` 文档 / `test:` 测试

```
refactor: 统一知乎搜索的错误处理

- 频率受限改为退避重试，不再直接抛错
- 其余错误统一抛 RuntimeError，不再返回错误码
- 重试次数与间隔提为模块常量
```

```
fix: 搜索失败时返回 error 字段以区分真实无结果
```